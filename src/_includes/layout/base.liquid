<!doctype html>
<html lang="en" class="text-lg sm:text-xl md:text-2xl no-js">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    {% if hidden == true %}
      <meta name="robots" content="noindex, nofollow">
    {% else %}
      <link rel="canonical" href="{% if canonical.url %}{{ canonical.url }}{% else %}{{ page.url | prepend: app.url | permalink }}{% endif %}">
    {% endif %}
    {% include "hashed-assets/scripts-head.liquid" %}
    {% include "hashed-assets/styles.liquid" %}
    <title>{% if type != 'homepage' %}{{ title | markdownify: 'inline' | strip_html }} | {% endif %}{{ app.title }}</title>
    <meta property="og:title" content="{% if title != '' %}{{ title | markdownify: 'inline' | strip_html }}{% else %}{{ app.title }}{% endif %}">
    <meta property="og:description" name="description" content="{% if summary %}{{ summary | markdownify | strip_html | strip_newlines | strip }}{% else %}{{ app.description }}{% endif %}">
    <meta property="og:url" content="{% if url %}{{ url }}{% else %}{{ page.url | prepend: app.url | permalink }}{% endif %}">
    {%- if image -%}
      <meta property="og:image" content="{{ image }}">
      <meta name="twitter:card" content="summary_large_image">
    {%- else -%}
      <meta property="og:image" content="{{ app.icon | prepend: app.url }}">
      <meta name="twitter:card" content="summary">
    {%- endif -%}
    <meta name="twitter:site" content="@isellsoap">
    <link rel="alternate" href="/feed.xml" type="application/atom+xml" title="Francesco Schwarz (RSS Atom Feed)">
    <link rel="alternate" href="/feed.json" type="application/json" title="Francesco Schwarz (JSON Feed)">
    <link rel="manifest" href="/app.webmanifest">
    <link rel="apple-touch-icon" href="{{ app.icon }}" type="image/jpg">

    <!-- meta: me -->
    <meta name="author" content="Francesco Schwarz">
    <link rel="me" href="mailto:francesco.schwarz@posteo.de">
    <link rel="me" href="https://twitter.com/isellsoap">
    <link rel="me" href="https://www.xing.com/profile/Francesco_Schwarz">
    <link rel="me" href="https://www.linkedin.com/in/francescoschwarz">
    <link rel="me" href="https://github.com/isellsoap">
  </head>
  <body class="bg-gray-200 text-gray-800 font-sans antialiased leading-normal h-card">
    <header role="banner" class="py-3 md:py-6">
      <div class="m-auto max-w-xl lg:max-w-2xl px-3 sm:px-6 lg:pl-12 flex items-center">
        <img src="/images/app.jpg" class="h-16 w-16 lg:h-24 lg:w-24 shadow-md rounded-full u-photo" alt="Francesco Schwarz">
        <div class="flex-grow ml-3 md:ml-6">
          <h1 class="text-lg mb-1 font-semibold leading-tight p-name"><a href="{{ '/' }}" class="text-inherit hover:text-gray-900 focus:text-gray-900 no-underline u-url">{{ app.title }}</a></h1>
          <nav role="navigation">
            <ul class="list-none m-0 flex flex-wrap text-gray-600">
              {% for item in navigation.primary %}
                <li class="{% unless forloop.last %}mr-2 md:mr-3 lg:mr-4{% endunless%}"><a href="{{ item.url }}">{{ item.label }}</a></li>
              {% endfor %}
            </ul>
          </nav>
        </div>
      </div>
    </header>

    <main role="main" class="block">
      <div class="m-auto max-w-xl lg:max-w-2xl pt-6 p-3 sm:p-6 lg:pl-12 lg:pr-16 bg-white">
        {{ content }}
      </div>
    </main>

    <footer role="contentinfo" class="py-3 md:py-6 text-gray-600">
      <div class="m-auto max-w-xl lg:max-w-2xl px-3 sm:px-6 lg:pl-12 footer-inner">
        <ul class="list-none flex flex-wrap ml-0">
          {% for item in navigation.secondary %}
            <li class="{% unless forloop.last %}mr-3 lg:mr-4{% endunless%}"><a href="{{ item.url }}">{{ item.label }}</a></li>
          {% endfor %}
        </ul>
        <p class="italic p-note">I’m Francesco Schwarz, a Frontend Developer based in Munich, Germany. My passion is the web, and I love to think and talk about HTML, CSS, and JavaScript (in that order). Get in touch with me via <a href="https://twitter.com/isellsoap">Twitter</a>, <a href="https://github.com/isellsoap">GitHub</a>, <a href="https://www.linkedin.com/in/francescoschwarz">LinkedIn</a> or <a class="u-email" href="mailto:francesco.schwarz@posteo.de">email</a>.</p>
        <p class="mb-0">This is the very bottom of the website. <span class="px-1">🙌</span> Thanks for stopping by!</p>
      </div>
    </footer>
  </body>
</html>
